import CrystalScene from '../components/crystal/CrystalScene'

// Phase 4 builds the full Entry Scene (ring text, shatter, input).
// Phase 3: crystal only — no HTML overlay.
export default function EntryScene() {
  return <CrystalScene />
}
