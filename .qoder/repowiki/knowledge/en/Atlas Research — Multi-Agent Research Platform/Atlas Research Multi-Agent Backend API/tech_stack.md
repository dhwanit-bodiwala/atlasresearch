FastAPI + Uvicorn ASGI server, Pydantic v2 for request validation, psycopg2-binary for PostgreSQL access with pgvector vector similarity search, Ollama Python client for local LLM inference (qwen3 8b/14b models), LangChain HuggingFace embeddings (`all-MiniLM-L6-v2`), DuckDuckGo search via `ddgs`, Trafilatura for web page extraction, PyPDF for PDF parsing, and httpx for the chat endpoint's direct Ollama HTTP call.